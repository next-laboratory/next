<?php

namespace PHPSTORM_META {
    override(\Psr\Container\ContainerInterface::get(0), map('@'));
    override(\Psr\Container\ContainerInterface::make(0), map('@'));
    override(\Max\Di\Container::make(0), map('@'));
    override(\Max\Di\Container::make(0), map('@'));
    override(\make(0), map('@'));
}
